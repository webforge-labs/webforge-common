language: php
php:
  - 5.3
  - 5.4

branches:
  only:
    - master
    - wip

before_script:
  - composer self-update
  - COMPOSER_ROOT_VERSION=dev-master composer install --dev --prefer-dist

script: phpunit

notifications:
  irc: "irc.freenode.org#webforge"
  mail: p.scheit@ps-webforge.com